import 'package:flutter/material.dart';

class ExamScreenBody extends StatelessWidget {
  const ExamScreenBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Exam Screen"),);
  }
}
